# 2026-09-15

## 应用图标改为 PNG 位图（提交 fc40931）

- 需求：把矢量自适应图标改成位图（mdpi~xxxhdpi 五档全套），蓝色底 #1E54E5。
- 结果：`res/mipmap-{m,h,xh,xxh,xxxh}dpi/ic_launcher.png` 与 `ic_launcher_round.png` 共 10 个 PNG；
  `mipmap-anydpi-v26/*.xml`、`drawable/ic_launcher_foreground.xml`、`mipmap/ic_launcher_background.xml` 全部删除。
- Manifest 未改动：`android:icon="@mipmap/ic_launcher"` + `android:roundIcon="@mipmap/ic_launcher_round"` 现在自动解析到 PNG。
- 尺寸按自适应图标 108dp 画布换算：mdpi 108 / hdpi 162 / xhdpi 216 / xxhdpi 324 / xxxhdpi 432。
  选择 108dp 基准而非传统 48dp 基准，是因为画布构图（车形占 67% 宽度）与原自适应图标完全一致，视觉比例不变。

### 关键坑：.gitignore 里的全局 `*.png` 会挡住图标
- 仓库 `.gitignore` 有 `*.png`（用于排除截图），新生成的 mipmap PNG 会被静默忽略、根本进不了提交。
- 需要在文件末尾加例外（父目录未被排除，负向规则才生效）：
  ```
  !app/src/main/res/mipmap-*/ic_launcher.png
  !app/src/main/res/mipmap-*/ic_launcher_round.png
  ```
- 验证手段：`git status --porcelain -uall`，确认 PNG 以 `??` 列出而不是被吞掉。

### 可复用：矢量图标栅格化流程
- 脚本：`tools/gen-icons/gen_icons.py`（矢量源 `tools/gen-icons/ic_launcher_foreground.xml` 同目录留档）。
- 原理：VectorDrawable 的 `android:pathData` 与 SVG 的 `d` 语法完全兼容，`<group>` 的
  pivot/scale/rotate/translate 能换算成等价的 SVG transform，所以改用无头浏览器渲染即可得到矢量级清晰的位图，
  不需要装 cairo 之类的原生依赖。
- group 变换换算（重点）：Android 的 `postTranslate/postScale` 是**左乘**语义（与 iOS 相反），
  变换链为 `T(translate+pivot) · R(rotation) · S(scale) · T(-pivot)`，pivot 点保持不动。
  对应的 SVG 写法：`transform="translate(tx+px,ty+py) rotate(r) scale(sx,sy) translate(-px,-py)"`。
  ⚠️ 顺序写反会导致图形偏移（把 pivot 当成普通缩放原点就会算错位置）。
- 渲染命令（Edge/Chrome 无头，WSL 之外直接可用）：
  ```
  msedge --headless=new --disable-gpu --hide-scrollbars --force-device-scale-factor=1 \
         --default-background-color=00000000 --window-size=432,432 \
         --screenshot=out.png file:///.../icon.html
  ```
  `--force-device-scale-factor=1` 必须有，否则系统 DPI 缩放会让输出尺寸翻倍。
  `--default-background-color=00000000` 用于圆形变体保留透明角。
- 环境：本机 Edge 在 `C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe`；
  Pillow 装在 `C:\Users\Administrator\.workbuddy\binaries\python\envs\default`（用于校验输出尺寸）。

## 新增「退出应用」功能

- 入口位置：**设置页最底部**一张卡片（红色电源图标 + 说明 + 全宽危险色按钮），点击弹 `ConfirmDialog` 二次确认后退出。
- 未加到顶部菜单栏；如果用户觉得藏太深，可在 `res/menu/main_menu.xml` 再补一个 `menu_exit` 项复用同一逻辑。
- 新增 `com.carassistant.util.AppExit`，退出顺序**不能改**：
  1. `stopService(MonitorService)` —— 性能监控是**前台服务**，不先停就杀进程的话系统会把它重新拉起，表现为"退出了但悬浮窗又冒出来"；
  2. `NotificationManagerCompat.cancelAll()` —— 清掉前台服务通知与 U 盘提醒；
  3. `finishAndRemoveTask()` —— 关掉整个任务栈（含压栈的设置页/详情页）；
  4. `Handler.postDelayed(250ms)` 后再 `killProcess(myPid()) + System.exit(0)` —— 留时间让 stopService 真正走完 `onDestroy`。
- 复用件：`ConfirmDialog.show(...)` + `R.drawable.ic_power`（新建的 24dp 电源矢量，dialog 里由 `android:tint="#FFFFFF"` 染白）。
  32 位色值写法沿用项目惯例 `#FF000000`。
- 提交待补（见 git log）。

## 图标换成用户提供的 192x192 成品设计稿（提交 fd93494）

- 用户给了 `Downloads/192.zip`（内含 `192.png`，192×192 RGBA，蓝色圆角方块 + 白色车形），要求用它做图标。
- 已归档为**唯一设计源**：`tools/gen-icons/source/ic_launcher_source.png`。
- **密度基准变了**：从原来的 108dp 自适应画布（108/162/216/324/432）改成传统图标标准 48dp 基准
  （**mdpi 48 / hdpi 72 / xhdpi 96 / xxhdpi 144 / xxxhdpi 192**）。
  理由：设计稿是自带圆角方块的**完整图标**（不是自适应前景层），192px 正好是标准 xxxhdpi 尺寸；
  若按 108dp 画布就得把 192 放大到 432，会发虚。
- 圆形变体（roundIcon）用超采样圆形遮罩裁切：先画 4 倍大的圆形 mask 再缩回来，边缘才平滑。
- 设计源内容 bbox = (0,0,192,192)，满画布无留白，桌面显示不会被无谓缩小。
- ⚠️ 分辨率上限就是 192px。若车机 launcher 把图标放大到 192 以上会发虚 —— 需要的话让用户提供
  512/1024 的设计稿再重跑脚本，**不要**直接上采样。
- `gen_icons.py` 已重构：优先读位图设计源；找不到才回退到原来的矢量渲染路径（无头浏览器）。
- ⚠️ `.gitignore` 的 `*.png` 又咬了一口：设计源 `tools/gen-icons/source/*.png` 需要单独补例外。

## Gradle 缓存锁的彻底解法（比 `--stop` 更管用）

- 现象：`gradlew assembleDebug` 启动阶段直接失败，报
  `Could not create service of type FileAccessTimeJournal` →
  `java.io.FileNotFoundException: C:\Users\Administrator\.gradle\caches\journal-1\journal-1.lock (拒绝访问)`。
- 之前记录的办法是 `gradlew --stop`，但这次**连跑两次 --stop + 重编都无效**，`--stop` 还报 "No Gradle daemons are running"。
- 真正的原因是 `.gradle\caches\journal-1` 里残留了一个**僵死的锁文件**（file-access.bin / journal-1.lock）。
- **有效解法**：直接删掉整个缓存目录再重编，立刻成功（Gradle 会自动重建）：
  ```powershell
  & .\gradlew.bat --stop
  [System.IO.Directory]::Delete("$env:USERPROFILE\.gradle\caches\journal-1", $true)
  & .\gradlew.bat assembleDebug
  ```
  删的是 Gradle 的**文件访问时间缓存**，不是项目数据，安全。
- 排查顺序：① `gradlew --stop`；② `Get-Process java` 看有无残留守护进程；③ 删 `journal-1` 目录。

## 排查「退出按钮看不到 + 图标还是旧的」= 包根本没装上

- 现象：用户反馈装到车机后，设置页找不到退出按钮，桌面图标还是旧的小图标。
- 先自查构建产物（**代码没问题**）：
  - `aapt2 dump resources app-debug.apk` 里能查到 `id/btn_exit_app`、`drawable/ic_power`、
    `string/settings_exit*`，说明退出功能确实打进去了；
  - `aapt2 dump badging` 显示 `application-label` 已是「应用管理」，
    `application-icon-160/240/320/480/640` 分别指向新生成的 mipmap-mdpi~xxxhdpi。
- **真因：签名不匹配，安装被系统拒绝**。
  - 车机上已装的旧版是公司 release 签名 `CN=Remobie Check, O=Chongzhu, C=TH`
    （对应 `Downloads\app-release.apk`，停在 **2026-09-03**，比所有改动都早）；
  - 本地 `assembleDebug` 是 `CN=Android Debug` 签名；
  - 签名不同的 APK 不能覆盖安装，Android 直接拒绝，旧应用原样留着 —— 所以"改了个寂寞"。
- 项目根没有 `keystore.properties` / `*.jks`，本地也造不出同签名的 release 包。
- 处理：把新包单独导出为 `dist/CarAssistant-slim-20260915.apk`（sha256 前 16 位 `7c5c59a7b946299e`，
  与 `app-debug.apk` 一致），避免用户再误拿 `Downloads` 里 09-03 的旧包。
  安装前必须先卸载旧的 `com.carassistant`。
- 待用户答复：是否有 Remobie/Chongzhu 的 release keystore；有的话走 `assembleRelease` 原地升级。

## 定位「模拟器上也没有退出按钮」= 设置页是孤岛（提交 d8bafe6）

- 现象：用户新装了当天的包，在雷电模拟器上仍反馈看不到退出按钮。这次包确认是新的（签名是 Android Debug、
  资源里有 `btn_exit_app`），所以不再是签名问题，是**代码问题**。
- `uiautomator dump` 首页 XML 里既没有设置节点、也没有齿轮——顺着查 `grep -rn SettingsActivity`，发现
  **全工程只有 MainActivity 会启动它，而且走的是 `onCreateOptionsMenu` + `res/menu/main_menu.xml`**。
  主题是 `NoActionBar`，选项菜单压根不渲染 → 设置页没有任何可达入口（精简版把原来的悬浮球区块删掉时，
  唯一那条路也断了）。
- 修法两条：
  1. 首页头部 `fragment_home.xml` 加齿轮 `btn_settings`，`HomeFragment` 里绑 `startActivity(SettingsActivity)`；
     同时删掉 MainActivity 的 `onCreateOptionsMenu/onOptionsItemSelected` 与 `res/menu/main_menu.xml`（死代码）。
  2. 退出按钮从设置页底部卡片**上移到设置页头部**（`activity_settings.xml` 的渐变头里，齿轮对侧），
     并删掉只被它引用的 `settings_exit_desc`。底部位置在 2560 屏上落在 y≈2224，车机横屏更够不到。
- 模拟器实机走查（`127.0.0.1:5555`，1440×2560，density 640）：
  首页齿轮 (1224,312) → 设置页头部退出 (1138,284) 首屏可见 → 二次确认弹窗（标题「退出应用」，
  正文说明后台悬浮监控会停） → 点「退出」(983,1691) → `pidof` 由 8543 变空、
  `top activity` 变 `com.android.launcher3/.Launcher`、`MonitorService` 计数 0。✅ 退出功能确认可用。
- 小疑点：退出后 `dumpsys notification` 仍显示 1 条通知，怀疑是 launcher/系统通知而非本应用前台通知泄漏，未深究。

## 推送与产物收尾

- `git push origin slim-core` → `8412e55..d8bafe6`（含本轮 6 个提交），远端已是最新。
- 顺带发现并记录一个环境坑：`refs/remotes/origin/slim-core` **写不进磁盘**，
  fetch/update-ref 都报成功但 `git for-each-ref refs/remotes/` 仍只有 main，
  于是 `git branch -vv` 永远显示 `[origin/slim-core: gone]`。纯显示问题，已写明用显式 refspec 推拉。
- 刷新交付包：`dist/CarAssistant-slim-20260915.apk`（7,125,216 字节，SHA256 前 16 位 `18B935C82BFC6A92`），
  即当前源码构建的 `app-debug.apk`。`aapt2` 复核包内确含 `id/btn_settings`、`id/btn_exit_app`、
  `drawable/ic_power`，label = 应用管理，图标 5 档 mipmap 齐全。
- 清理了本次会话的临时文件（`_b.log`、`_commitmsg.txt`、`_verify/`），工作区干净。

## 待办 / 待验证
- 车机上验证新包：必须先**卸载**旧的 `com.carassistant`（公司 release 签名，装不上 debug 包），再装
  `dist/CarAssistant-slim-20260915.apk`。位图图标若走旧式图标路径显得偏小，可调 `gen_icons.py` 的放大系数重生成。
- 想「原地升级不卸载」只能拿到 Remobie/Chongzhu 的 release keystore（放项目根 + 写 `keystore.properties`）。
- `versionCode` 仍是 20260802，从未随构建更新，走 release 前应改成构建日期 YYYYMMDD。

