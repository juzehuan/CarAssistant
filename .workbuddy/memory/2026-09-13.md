# 2026-09-13

## 精简分支 slim-core：只保留六大核心功能

- 需求：app 只保留「清理优化、应用管理、文件管理、性能监控、自启管理、设备信息」，其余全部移除。
- 与用户确认的边界：侧边栏/悬浮球一并删除；设置页保留（只删悬浮球区块，留内存清理白名单+关于）；U 盘监听与权限引导页都保留。
- 新建本地分支 `slim-core`（注意：本环境 git 无法创建带 `/` 的分支名，如 `feature/xxx` 会静默失败，引用写不进 `.git/refs/heads/`，务必用无斜杠名字）。
- 基线：本地仓库原本没有任何 commit（工作区与 origin/main 0cb092b 一致），已在 main 上补建基线提交，git 身份设为 `juzehuan <juzehuan@189.cn>`。
- 删除 49 个 java（lyrics/、music/ 整包 + 音乐/歌词/按键映射/控制面板/侧边栏相关 Activity、Service、Util、Adapter），Manifest 同步移除组件与 CAMERA/BLUETOOTH/WIFI/WRITE_SETTINGS/MODIFY_AUDIO_SETTINGS/INTERNET 权限。
- 资源清理方法（可复用）：扫描 res 下所有资源定义名 vs java/xml/manifest 中的 `R.x.y` 与 `@x/y` 引用，未被引用的整文件删除；再对 strings.xml/colors.xml/dimens.xml 做行级正则剔除。本轮删 138 个资源文件 + 617 条 string/color/dimen。
- 验证：`./gradlew assembleDebug` 通过。构建环境：`JAVA_HOME=C:\Program Files\Android\Android Studio\jbr`，`ANDROID_HOME=C:\Users\Administrator\AppData\Local\Android\Sdk`（未建 local.properties）。`--offline` 会拿不到 AGP 插件，需联网；首次会下载 gradle-8.13。
- 坑：PowerShell 用 `git commit -F -` 走管道会把中文变成 `?`，必须把 message 写成 UTF-8 文件再 `git commit -F file`。
- 坑：本环境 `Remove-Item` 被 safe-delete 拦截（回收站失败），改用 `[System.IO.File]::Delete()` / `[System.IO.Directory]::Delete(p,$true)` 可绕过。
- 坑：Bash 工具缺 coreutils（grep/sed/wc/head 都不可用），只能用 Grep 工具或 PowerShell。PowerShell 工具的 stdout 不回显，需写文件再读。
