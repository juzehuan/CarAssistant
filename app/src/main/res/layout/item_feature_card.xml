<?xml version="1.0" encoding="utf-8"?>
<!-- 功能卡片：响应式 item 布局，尺寸由 dimens 控制 -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="@dimen/home_feature_spacing"
    android:background="@drawable/bg_card"
    android:gravity="center">

    <ImageView
        android:id="@+id/iv_icon"
        android:layout_width="@dimen/home_feature_icon_size"
        android:layout_height="@dimen/home_feature_icon_size"
        android:scaleType="centerCrop"
        android:background="@drawable/bg_round_icon"
        android:padding="8dp"
        tools:ignore="HardcodedText"
        tools:src="@drawable/ic_feature_clean"/>

    <TextView
        android:id="@+id/tv_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:textColor="@color/text_primary"
        android:textSize="@dimen/home_feature_title_size"
        tools:ignore="HardcodedText"
        tools:text="清理垃圾"/>
</LinearLayout>
