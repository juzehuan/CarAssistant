<?xml version="1.0" encoding="utf-8"?>
<!--
    开机自启项列表项（优化版）
    - 序号徽章 + 图标 + 名称/包名 + 启动按钮 + 移除按钮
    - 启动按钮用品牌色实底，移除按钮用次级危险样式，主次分明
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingStart="@dimen/list_item_padding_h"
    android:paddingEnd="@dimen/list_item_padding_h"
    android:paddingTop="@dimen/list_item_padding_v"
    android:paddingBottom="@dimen/list_item_padding_v"
    android:layout_marginStart="2dp"
    android:layout_marginEnd="2dp"
    android:layout_marginTop="5dp"
    android:layout_marginBottom="5dp"
    android:background="@drawable/bg_app_item">

    <!-- 序号徽章：开机启动顺序，圆形品牌浅蓝底 -->
    <TextView
        android:id="@+id/tv_index"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:gravity="center"
        android:textColor="@color/brand_primary"
        android:textSize="12sp"
        android:textStyle="bold"
        android:background="@drawable/bg_status_disabled"
        tools:ignore="HardcodedText"
        tools:text="1"/>

    <ImageView
        android:id="@+id/iv_icon"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginStart="10dp"
        tools:src="@mipmap/ic_launcher"/>

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="14dp"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tv_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/text_primary"
            android:textSize="14sp"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end"
            tools:text="应用名称"/>

        <TextView
            android:id="@+id/tv_pkg"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:textColor="@color/text_hint"
            android:textSize="10sp"
            android:maxLines="1"
            android:ellipsize="end"
            tools:text="com.example.app"/>
    </LinearLayout>

    <Button
        android:id="@+id/btn_launch"
        style="@style/Widget.CarAssistant.Button.Primary.Small"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="6dp"
        android:text="@string/autostart_launch"/>

    <Button
        android:id="@+id/btn_remove"
        style="@style/Widget.CarAssistant.Button.Danger.Small"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/remove"/>
</LinearLayout>
